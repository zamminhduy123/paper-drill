## Question

What is the final `config/scope.yaml` schema (keywords, authors, theses, excludes, per_run/keep) so non-technical edits reliably steer ingestion?

Type: grilling
Status: open
Blocked by:
